# 上机作业说明

开发环境Clion，C++20版本

## Problem1

按照题目要求，我将员工数据（如姓名、月薪）和操作（计算薪资）封装在类的内部，并且FullTimeEmployee和PartTimeEmployee都继承自Employee基类；
使用 new 关键字在堆上动态创建员工对象，这便于构建多态数组。在程序末尾使用delete释放内存
重载了 << 运算符，使得可以直接用 cout << *employee[i]

不足之处在于，输入比较简陋：
```
全职员工 张三 8000 2000
全职员工 王五 7500 1500
兼职员工 李四 60 180
```
将以上内容输入可以得到题目中要求的输出结果。如果可以的话，可以设计一个输入解析函数，更加优雅。


### Problem2

主要是运算符的重载设计。

根据复数运算规则，设计了+、-、*三种运算。同时，在 << 的重构方面，考虑到复数类可以包括实数、纯虚数、虚部为正的数、虚部为负的数，最后实现优雅的输出。

使用常引用更加健壮。

直接编译运行即可得到标准的结果。

### Problem3

第三题的要点在于使用template。按照题意：

我成功地定义了 template <typename T> class Vector，从而可以用于 int 和 double 类型。
使用了T* arr 动态数组和 count 长度成员
实现 operator[]，可以访问修改数组元素。
重载了operator +，设计了两个版本，备注释的是，新Vector与较长的一致，目前使用的是Vector与较短的一致；
重载 == 运算符先检查两个向量的长度是否相等，然后逐一比较对应元素。
重载 << 优雅地显示Vector。
按照题目要求完成了主函数测试；
同时实现了add函数的自动扩容

输入：
```
3 3 2 2
1 2 3
4 5 6
1.5 2.5
1.5 2.5
```

最终结果符合要求


### Problem4

第4题要点在于函数重载与文件读取

按照题目要求实现了3中writeToFile，非常标准
最后依次读取3个文件，并且将读取内容作为字符串返回的函数也写得很标准

没有说清楚怎么输入数组，直接在main函数中定义整型数组、字符串、浮点数组

最后效果理想，直接运行即可。